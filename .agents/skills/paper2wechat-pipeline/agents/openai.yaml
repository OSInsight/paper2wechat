interface:
  display_name: "Paper2WeChat Pipeline"
  short_description: "Single-entry orchestration from arXiv input to WeChat-ready outputs"
  default_prompt: "Use $paper2wechat-pipeline to run end-to-end orchestration: parse paper, detect style evidence, resolve article markdown, and if markdown exists, publish with wechat-publisher (render, upload images, and optionally create draft)."
